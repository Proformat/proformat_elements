{{ macros.atomV1ButtonCss(design.przycisk.button, 'rm-button', '%%SELECTOR%%', breakpoint, globalSettings) }}
    
%%SELECTOR%% .read-more-container {
  {{ macros.layout(design.kontener.layout) }}
  {{ macros.spacing_all(design.kontener.spacing) }}
  {{ macros.borders(design.kontener.borders) }}
}

%%SELECTOR%% .readmore-primary-text, %%SELECTOR%% .readmore-expanded-text{
{{ macros.typography(design.tre_.typography, globalSettings) }}
}

%%SELECTOR%% .maria {
{{ macros.spacing_all(design.przycisk.spacing_all) }}
}


%%SELECTOR%% p {
  margin-block-start: {{ design.tre_.paragraph_margin_top.style }};
  margin-block-end: {{ design.tre_.paragraph_margin_bottom.style }};
}
%%SELECTOR%% th {
  {{ macros.typography(design.typography.table_headings, globalSettings) }}
}

%%SELECTOR%%:hover th {
  {{ macros.typographyWithHoverableColorAndEffects(design.typography.table_headings) }}
}